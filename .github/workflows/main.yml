name: jenkis_int
on:
  push:
  workflow_dispatch:
jobs:
  myfirstjob:
    runs-on: ubuntu-latest
    steps:
      - name: checkoutcode
        uses: actions/checkout@v5
      - name: shellscript
        run: echo "hello my first integration"
      - name: set up java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'
      - name: connect to lab server
        uses: appleboy/ssh-action@v0.1.10
        with:
            host: 3.110.111.224
            username: "labuser"
            password: "Nuvelabs123$"
            port: 22
            first_ssh: |
                curl -l -u admin:Root123$ http://localhost:8080/job/project1_devops/build?token=balu
            last_ssh: |
                cd ./target && ls -l
      
